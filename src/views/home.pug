extends ./main-layout.pug


block main-layout__header__menu
    ul.main-layout__menu
        a( href="/" )
            li( class="active" ) Home
        if userEmail
            a( href="/product/new" )
                li Add Product
                style.
                    .main-layout__menu {
                        grid-template-columns: 100px 120px 1fr 100px 100px 100px;
                    }
        div
        a( href="/register" )
            li Register
        a( href="/login" )
            li Login
        a( href="/logout" )
            li Logout

block main-layout__block
    h1 Home